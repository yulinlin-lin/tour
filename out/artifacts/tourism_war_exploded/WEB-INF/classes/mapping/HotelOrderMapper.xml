<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sgu.tourism.mapper.IHotelOrderMapper">

    <insert id="insertObject" parameterType="com.sgu.tourism.entity.HotelOrder">
        insert into t_hotel_order(user_name, user_phone, id_number, user_sex, user_note, hotel_title, orderNumbers, total_price, orderUser_name, create_time)
        VALUES (#{userName},#{userPhone},#{idNumber},#{userSex},#{userNote},#{hotelTitle},#{orderNumbers},#{totalPrice},#{orderUserName},#{createTime})
    </insert>

</mapper>